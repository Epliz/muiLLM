General ideas:
--------------

- try push vs pulll
- splitting kernels to start next one
- fusing kernels to avoid dispatch overhead


GEMM-RS:
--------

- try splitting for large matrices:
  - spawn the scatter kernels first, wait inside it
- improving further reduction kernels
- use pull approach to be able to do reduction during data transfers
    - potentially use shared memory, pulling from one GPU at a time and accumulating in there
    - start from different GPUs
    - reduction for large transferes is 10%
    - could fuse wait in transfer kernel as well

All2All:
--------

- reduce gpu syncs: DONE
   maybe the issue we overwrite counters?
   use more counter sets

- fuse gpu syncs
- avoid global atomics in as many kernels as possible

- reduce CPU overhead:
  - all in C++ DONE
  - remove torch::zeros DONE
  - remove torch::slice DONE
  - monkey patch distributed barrier
  - re-use torch tensors
- remove as many kernels as possible 
  -> for both dispatch and combine avoid "compute_send_counts" kernels
     by writing from sender to dst directly based on token_idx/expert_idx
     - DONE for combine
     - for dispatch, could use global atomics in send + write at a per rank place

- vectorized copies everywhere
  all2all_dispatch_pack_send_buffers_fp32_kernel
  all2all_dispatch_pack_send_buffers_fp16_kernel
  all2all_dispatch_unpack_fp16_kernel DONE
  all2all_dispatch_unpack_fp32_kernel DONE
  compute
  all2all_combine_pack_send_buffers_fp32_kernel (revisit)

- fuse local count caching with GPU barrier: DONE
- check if divisions are a problem (can run a profile removing them, then if yes, use the magic int division)
   (e.g. https://github.com/milakov/int_fastdiv/blob/master/int_fastdiv.h)

- avoid checking expert_num_tokens
  - in compute -> not beneficial for big sizes, doesn't change anything for small sizes
  - in dispatch unpack
  - in combine pack send


Most important:
---------------
- remove dispatch count
- fuse compute?